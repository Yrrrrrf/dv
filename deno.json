{
	"name": "@yrrrrrf/dv",
	"version": "0.0.3",
	"description": "Commands, composed. A Deno CLI and TypeScript library for discovery, heterogeneous command matrices, preparation, execution, and grouped terminal reporting.",
	"author": "Fernando Bryan Reza Campos <fer.rezac@outlook.com>",
	"readme": "README.md",
	"license": "MIT",
	"exports": { ".": "./src/mod.ts", "./cli": "./src/cli/mod.ts" },
	"tasks": {
		"check": "deno fmt --check && deno lint && deno check src/mod.ts example/pipeline.ts",
		"test": "deno test -A test/",
		"coverage": "deno test -A --coverage=coverage test/",
		"dv": "deno run -A src/mod.ts"
	},
	"publish": {
		"include": ["src/", "README.md", "LICENSE", "deno.json"]
	}
}
